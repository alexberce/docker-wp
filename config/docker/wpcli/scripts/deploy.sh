#!/bin/bash

docker-compose up --build -d
#alias wp="docker-compose run --rm wpcli"

#cd website
#wp core install --url="localhost:8080" --title="WordPress" --admin_user="admin" --admin_password="admin" --admin_email="admin@email.com"